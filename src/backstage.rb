class Backstage
  def update(quality, sell_in)
    # sell_in.reduce
    #
    # quality.upgrade_by(2) if sell_in.value <= 10
    # quality.upgrade_by(3) if sell_in.value <= 5
    # quality.value = 0  if sell_in.value <= 0
  end
end